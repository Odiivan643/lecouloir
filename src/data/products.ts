// Ce fichier ne contient plus de données mock.
// Les produits sont désormais chargés depuis le backend FastAPI (GET /api/products).
// Gardé pour compatibilité d'import — ne pas y ajouter de données en dur.
import { Product } from '../types';
export const INITIAL_PRODUCTS: Product[] = [];
